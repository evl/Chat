local addonName, addon = ...
local config = addon.config

--config.anchorEditBoxTop = false -- Anchor edit box to the top of the chat window
--config.hideEditBoxBackground = false -- Hide edit box background when active
--config.hideEditBoxFocus = false -- Hide edit box focus when active
--config.hideNavigationButtons = true -- Hide up, down and button buttons
--config.hideMenuButton = true -- Hide the new chat menu button
--config.hideSocialButton = true -- Hide the new social button introduced in 3.3.5

-- Configuration for a typical "spam" window
--ChatFrame3.repeatFilter = true -- Repeated messages from the same sender are ignored
--ChatFrame3:SetTimeVisible(10) -- Fade messages after 10 seconds (default: 120 seconds)